package com.example.tute5.volunteering.services

class AvgFun {

    companion object {
        fun avgHoursPerGarden(amount: Int, count: Int): Int {
            return amount/count;
        }
    }
}